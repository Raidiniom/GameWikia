import { UpdStyle } from "@/styles/screens/updstyle";
import { ScrollView, Text } from "react-native";

export default function UmamusumePrettyDerby() {
    return (
        <ScrollView style={UpdStyle.bodyContainer} >
            <Text>Umamusume Pretty Derby</Text>
        </ScrollView>
    );
}