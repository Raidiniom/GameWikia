import { GiStyle } from "@/styles/screens/gistyle";
import { ScrollView, Text } from "react-native";

export default function GenshinImpact() {
    return (
        <ScrollView style={GiStyle.bodyContainer} >
            <Text>Genshin Impact</Text>
        </ScrollView>
    );
}