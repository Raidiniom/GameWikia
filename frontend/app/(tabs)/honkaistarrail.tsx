
import { ScrollView, Text } from "react-native";

export default function HonkaiStarRail() {
    return (
        <ScrollView  >
            <Text>Honkai Star Rail</Text>
        </ScrollView>
    );
}